# 计算机网络全栈复习：从浏览器输入 owen571.top 到博客页面呈现

以 https://owen571.top/study/随心博客/MCP和A2A--Agent的横向与纵向沟通/ 为例，
追踪一个 HTTP 请求从用户浏览器到服务器、再返回的完整路径，
覆盖每一层涉及的协议和机制。

本文对应面试常见题："从输入 URL 到页面加载完成，发生了什么？"


## 一、总览：数据包的逐层穿行

用户浏览器                                    服务器 (101.35.20.131)
    │                                                  │
    │  ① DNS (UDP 53)                                  │
    │  owen571.top → 101.35.20.131                     │
    │                                                  │
    │  ② TCP 三次握手 (端口 443)                        │
    │  SYN → SYN-ACK → ACK                             │
    │                                                  │
    │  ③ TLS 1.3 握手                                  │
    │  证书验证 + 密钥协商                               │
    │                                                  │
    │  ④ HTTP/2 (ALPN 协商)                            │
    │  GET /study/... HTTP/2                           │
    │  Host: owen571.top                               │
    │────────────────────────────────────────────────→│
    │                                                  │ ⑤ Caddy (反向代理)
    │                                                  │   路由匹配 → 127.0.0.1:4321
    │                                                  │
    │                                                  │ ⑥ Astro Node SSR
    │                                                  │   Markdown → HTML
    │                                                  │
    │  ⑦ HTTP/2 Response (zstd 压缩)                   │
    │←────────────────────────────────────────────────│
    │                                                  │
    │  ⑧ 浏览器解析 HTML、加载子资源                     │
    │  ⑨ TCP 四次挥手（或 Keep-Alive 复用）              │


## 二、逐层拆解

### 2.1 DNS 解析 —— 把域名变成 IP

涉及协议：DNS (应用层) → UDP (传输层) → IP (网络层) → 以太网/Wi-Fi (链路层)

用户在地址栏输入 owen571.top，浏览器第一步要找到这个域名对应的 IP 地址：

1. 浏览器自身 DNS 缓存（chrome://net-internals/#dns）
2. 操作系统 DNS 缓存（Linux: systemd-resolved 或 nscd）
3. 本地 hosts 文件（/etc/hosts）
4. DNS 递归查询（由运营商 DNS 或公共 DNS 如 8.8.8.8 完成）：

   owen571.top
     │
     ▼
   根域名服务器（Root DNS）
     │  返回 .top 顶级域名的权威 DNS 地址
     ▼
   .top 顶级域名服务器（TLD DNS）
     │  返回 owen571.top 的权威 DNS 地址
     ▼
   owen571.top 权威 DNS（腾讯云 DNSPod）
     │  返回 A 记录: owen571.top → 101.35.20.131
     ▼
   浏览器得到 IP 地址

关键细节：
- DNS 查询默认走 UDP 端口 53，单次查询 ≤512 字节
- 超过 512 字节或区域传输时切换为 TCP 53
- 现代浏览器可以同时发 DNS over HTTPS (DoH, RFC 8484) 到 https://dns.google/dns-query
- 查询结果带 TTL（Time To Live），缓存到期前不再重复查询
- 递归查询 vs 迭代查询：本地 DNS 服务器做递归（帮用户查到底），
  根/TLD/权威 DNS 只做迭代（告诉你去哪查，不替你查）

面试追问：A 记录和 AAAA 记录的区别？
→ A 返回 IPv4 地址（32 位），AAAA 返回 IPv6 地址（128 位）。
  CNAME 是别名记录，把域名指向另一个域名而非直接指向 IP。


### 2.2 TCP 三次握手 —— 建立可靠连接

涉及协议：TCP (传输层)

目标端口：443（HTTPS 标准端口）

协议栈位置（封装关系，自底向上）：

  链路层帧头  IP 头  TCP 头  数据（TLS 握手内容）  帧尾 FCS
  ┌─────────┬───────┬───────┬────────────────────┬─────────┐
  │ 14 字节  │20 字节│20 字节│     可变长度         │ 4 字节   │
  └─────────┴───────┴───────┴────────────────────┴─────────┘

三次握手：

  Client (浏览器)                    Server (101.35.20.131:443)
      │                                        │
      │── SYN, seq=x ────────────────────────→│  ① SYN: 客户端选一个初始序号 x，
      │    TCP 标志位: SYN=1, ACK=0             │     不带应用数据，纯控制报文
      │                                        │     TCP 头 20 字节
      │                                        │
      │←─ SYN+ACK, seq=y, ack=x+1 ────────────│  ② SYN+ACK: 服务器选序号 y，
      │    TCP 标志位: SYN=1, ACK=1             │     同时确认收到 x
      │                                        │
      │── ACK, seq=x+1, ack=y+1 ─────────────→│  ③ ACK: 客户端确认收到 y
      │    TCP 标志位: SYN=0, ACK=1             │     此时客户端进入 ESTABLISHED
      │                                        │     服务器收到后也进入 ESTABLISHED

为什么是三次，不是两次或四次？

- 两次不够：假设第一次 SYN 在网络中滞留，客户端超时后重发了一个新 SYN。
  如果只有两次握手，服务器收到旧 SYN 就直接进入 ESTABLISHED 状态，
  分配资源等待数据，但客户端根本不知道——这是"半开连接"攻击（SYN Flood 的基础）

- 四次没必要：SYN+ACK 合并为一次即可完成双向初始化，多余一次纯浪费

TCP 头关键字段（都在上面的 SYN 包里）：
- 源端口（16 位）：浏览器随机选一个临时端口，如 54321
- 目的端口（16 位）：443
- 序号（32 位）：即上面的 seq=x
- 确认号（32 位）：即上面的 ack=x+1
- 窗口大小（16 位）：通告对方自己的接收缓冲区剩余空间（流控核心）
- 标志位（6 位）：URG / ACK / PSH / RST / SYN / FIN

### 2.3 TLS 握手 —— 在 TCP 之上建立加密隧道

涉及协议：TLS 1.3 (表示层 / 应用层之间)

Caddy 的 Caddyfile 没有显式配证书路径，因为 Caddy 内置 ACME 客户端，
自动向 Let's Encrypt 申请和续期免费 TLS 证书（90 天有效期）。

TLS 1.3 握手（简化版，1-RTT）：

  Client                                    Server
    │── ClientHello ───────────────────────→│
    │   支持的加密套件（TLS_AES_128_GCM_SHA256 等）
    │   密钥交换参数（DH 或 ECDHE 的公钥）
    │   SNI: owen571.top（服务器名指示，用于虚拟主机）
    │   ALPN: h2, http/1.1（应用层协议协商）
    │                                        │
    │←─ ServerHello ────────────────────────│
    │   选定的加密套件
    │   服务器的 DH 密钥交换参数
    │   ALPN: h2（服务器选了 HTTP/2）
    │←─ EncryptedExtensions ────────────────│
    │←─ Certificate ────────────────────────│
    │   X.509 证书链: Let's Encrypt R3 → ISRG Root X1
    │   包含公钥、域名 owen571.top、有效期
    │←─ CertificateVerify ──────────────────│
    │   用证书私钥签名，证明服务器持有私钥
    │←─ Finished ───────────────────────────│
    │                                        │
    │── Finished ───────────────────────────→│
    │                                        │
    │═══════ 后续全部加密通信 ════════════│

TLS 1.3 相比 1.2 的改进：
- 握手从 2-RTT 降到 1-RTT（省了一个往返，延迟减半）
- 移除了所有不安全的加密算法（RSA 密钥交换、CBC 模式、SHA-1、MD5）
- 支持 0-RTT 恢复（重连时第一个包就能带数据，但可能被重放攻击）

关键概念：

  SNI (Server Name Indication)：一个 IP 托管多个 HTTPS 域名时，
  ClientHello 中指明要访问 owen571.top，服务器返回对应证书。
  没有 SNI 的话，一个 IP 只能部署一个 HTTPS 站点。

  ALPN (Application-Layer Protocol Negotiation)：TLS 握手时协商
  上层用什么协议。浏览器说"我支持 h2 和 http/1.1"，服务器说"选 h2"，
  后续就建 HTTP/2 连接——不需要额外往返。

  证书信任链：
  owen571.top 证书 ← 签发者: Let's Encrypt R3 (中间 CA)
                   ← 签发者: ISRG Root X1 (根 CA，预装在操作系统/浏览器中)
  浏览器验证链条：每一级的签名都对上 → 根证书在自己信任列表里 → 通过。


### 2.4 HTTP 请求 —— 到达应用层

涉及协议：HTTP/2 (应用层)

由于 ALPN 协商选了 h2，浏览器使用 HTTP/2 发送请求。
HTTP/2 的核心改进（对比 HTTP/1.1）：

- 多路复用（Multiplexing）：一个 TCP 连接上同时跑多个请求/响应，
  不再有 HTTP/1.1 的队头阻塞（HOL Blocking）
- 二进制分帧（Binary Framing）：不再是文本协议，解析更快
- 头部压缩（HPACK）：静态表 + 动态表 + 哈夫曼编码，
  同一连接的请求头大幅压缩
- 服务器推送（Server Push）：服务器可以主动推送资源（已逐渐弃用）

一个典型的 HTTP/2 请求（逻辑表示，底层是二进制帧）：

  :method: GET
  :path: /study/随心博客/MCP和A2A--Agent的横向与纵向沟通/
  :authority: owen571.top
  :scheme: https
  accept: text/html,application/xhtml+xml
  accept-encoding: gzip, deflate, br, zstd
  accept-language: zh-CN,zh;q=0.9
  user-agent: Mozilla/5.0 ...

注意 URL 中的中文路径。浏览器会对其做 **百分号编码（Percent-Encoding）**：

  /study/随心博客/MCP和A2A--Agent的横向与纵向沟通/
  → /study/%E9%9A%8F%E5%BF%83%E5%8D%9A%E5%AE%A2/MCP%E5%92%8CA2A--Agent%E7%9A%84%E6%A8%AA%E5%90%91%E4%B8%8E%E7%BA%B5%E5%90%91%E6%B2%9F%E9%80%9A/

编码规则：UTF-8 编码后每个字节转为 % + 十六进制。比如"随"→ UTF-8 的 3 字节 E9 9A 8F → %E9%9A%8F。


### 2.5 反向代理：Caddy 的路由和转发

涉及协议：HTTP/2 接收 → HTTP/1.1 转发（Caddy 到后端通常用 HTTP/1.1）

Caddy 监听 0.0.0.0:443，收到请求后按 Caddyfile 规则匹配：

  1. 匹配域名 → owen571.top
  2. 路径不是 /api/* 不是 /ui/* 不是静态资源 → 走默认 handle 块
  3. reverse_proxy 127.0.0.1:4321

Caddy 做了几件事：
- TLS 终止（Termination）：Caddy 解密 TLS，后端 Astro 收到的已经是明文 HTTP
- 协议转换：外部 HTTP/2 → 内部 HTTP/1.1（因为 Node.js HTTP 服务器默认 HTTP/1.1）
- 压缩：encode zstd gzip → 响应回来时压缩（见 2.7 节）
- 静态资源缓存优化：对 /assets/ 等路径加 Cache-Control 头，max-age=2592000（30 天）

为什么用反向代理？

  1. TLS 终止：证书管理集中在 Caddy，后端服务不需要自己处理 HTTPS
  2. 负载均衡（当前单机没用上，但架构预留）
  3. 静态资源缓存策略统一管理
  4. 安全隔离：后端只监听 127.0.0.1，外网无法直连
  5. 压缩：Caddy 在代理层统一压缩，后端不需要处理 Gzip/Zstd


### 2.6 应用服务：Astro SSR 渲染

涉及协议：HTTP/1.1 (Caddy → Node.js)

owen-blog.service 通过 systemd 运行：
  → scripts/prod-start.sh
  → node ./dist/server/entry.mjs
  → 监听 127.0.0.1:4321

请求到达后的处理流程：

  1. Astro 收到 GET /study/随心博客/MCP和A2A--Agent的横向与纵向沟通/
  2. 路由匹配 → 找到对应的 Markdown 文件
     src/content/posts/study/随心博客/MCP和A2A--Agent的横向与纵向沟通.md
  3. 读取 Markdown 文件内容
  4. 通过 remark 插件链（remark-math → remark-directive → remark-sectionize → ...）
     将 Markdown 解析为 MDAST（Markdown Abstract Syntax Tree）
  5. 通过 rehype 插件链（rehype-katex → rehype-slug → rehype-autolink-headings → ...）
     将 MDAST 转为 HAST（HTML AST），再序列化为 HTML
  6. 将 HTML 嵌入 Svelte 布局组件，生成完整页面
  7. 返回 HTTP/1.1 200 OK + text/html

  Astro 输出模式是 "server" (SSR)，不是静态导出——每次请求都在服务端
  动态渲染，适合内容经常更新的博客。


### 2.7 响应回程：压缩 → 加密 → 传输

Caddy 收到 Astro 的 HTML 响应后：

  1. 检测 Accept-Encoding 中有 zstd 或 gzip
  2. 对响应体做 zstd 或 gzip 压缩（zstd 优先，因为压缩率更高、速度更快）
  3. HTML 文本压缩率通常 70-80%（比如 50KB → 12KB）
  4. 通过 TLS 层加密
  5. TCP 分段发送（MSS 通常 1460 字节）
  6. 每个 TCP 段封装为 IP 数据包
  7. IP 数据包封装为以太网帧，从网卡发出

压缩算法对比：

  zstd (Zstandard)：Facebook 开发，压缩率和高压缩级别下接近 xz，
    解压速度极快（> 1GB/s），Caddy 内置支持
  gzip：最广泛兼容（所有浏览器都支持），压缩率中等
  brotli (br)：Google 开发，压缩率高，解压快，但仅 HTTPS 可用


### 2.8 浏览器端：解析、渲染、子资源加载

浏览器收到 HTML 后：

  1. 解析 HTML → 构建 DOM 树
  2. 遇到 <link> / <script> / <img> → 发起子资源请求
  3. 对同一域名（owen571.top），HTTP/2 多路复用现有的 TCP 连接，
     不必再建新连接。新请求的头部被 HPACK 压缩（动态表已有之前的字段）
  4. 解析 CSS → 构建 CSSOM 树
  5. DOM + CSSOM → 渲染树（Render Tree）
  6. 布局（Layout）→ 计算每个元素的位置和尺寸
  7. 绘制（Paint）→ 光栅化 → 合成 → 显示到屏幕

子资源可能来自不同域名（如 Google Fonts、Umami 分析），
每个新域名需要独立的 DNS 解析 + TCP + TLS（如果走 HTTPS）。
HTTP/2 的连接复用只对同一 authority 有效。


## 三、所有涉及协议的速查表

  层        协议                      关键端口   核心作用
  ─────────────────────────────────────────────────────────
  应用层    DNS                       53 (UDP)  域名→IP
            HTTP/1.1                  —         请求-响应（文本协议）
            HTTP/2                    —         多路复用、头部压缩、二进制帧
            ACME (Let's Encrypt)      —         自动签发/续期 TLS 证书
            WebSocket                 —         升级自 HTTP，全双工通道（本博客暂未用）

  表示层    TLS 1.3                   —         加密、身份验证、完整性校验
            ALPN                      —         协商上层用 HTTP/1.1 还是 HTTP/2
            SNI                       —         一个 IP 多 HTTPS 域名的证书选择

  传输层    TCP                       任意       可靠字节流、拥塞控制、流量控制
            UDP                       任意       无连接、低开销（仅 DNS 用到）

  网络层    IP (IPv4)                  —         寻址和路由，逐跳转发
            ICMP                       —         差错报告、ping/traceroute
            ARP                        —         IP → MAC 地址映射（局域网内）

  链路层    以太网                     —         局域网帧传输
            Wi-Fi (802.11)            —         无线局域网


## 四、面试常见追问

Q: TCP 和 UDP 的核心区别？
A: TCP 面向连接（三次握手）、可靠（序号+确认+重传）、有流量控制和拥塞控制、
   面向字节流。UDP 无连接、不保证送达、无流控、面向报文、头部仅 8 字节。
   典型应用：TCP → HTTP/SSH/FTP；UDP → DNS/视频通话/游戏/QUIC。

Q: HTTP/1.1 的队头阻塞（HOL Blocking）是什么？HTTP/2 如何解决？
A: HTTP/1.1 在一个 TCP 连接上请求必须按顺序排队——前一个请求的响应没到，
   后面的请求只能等。这叫队头阻塞。HTTP/2 用多路复用在应用层解决了这个问题：
   多个请求/响应的帧可以在同一个 TCP 连接上交叉传输。但 TCP 层的队头阻塞仍在：
   一个 TCP 段丢了，后续所有段都要等重传。HTTP/3 (QUIC) 彻底解决了这一点。

Q: HTTPS 为什么安全？CA 的作用是什么？
A: HTTPS = HTTP + TLS。TLS 提供三样保障：
   机密性（对称加密，如 AES-128-GCM）、完整性（AEAD 认证加密，篡改会被发现）、
   身份验证（证书链 + CA 签名）。
   CA（Certificate Authority，证书颁发机构）验证域名所有权后签发证书，
   浏览器信任预装的根 CA（如 ISRG Root X1），通过签名链验证网站身份。

Q: TCP 的拥塞控制算法有哪些？
A: 经典的有 Reno（快速重传+快速恢复）、CUBIC（Linux 默认，适合高带宽长距离）、
   BBR（Google 开发，基于带宽和 RTT 建模，不依赖丢包信号）。
   核心机制：慢启动（cwnd 指数增长）→ 拥塞避免（线性增长）→
   丢包检测（超时或三次重复 ACK）→ cwnd 减半或更激进的调整。

Q: 从这台服务器 ping 百度，经过了哪些网络设备？
A: 从服务器的网络栈出发：应用层 ping 命令 → ICMP Echo Request → IP 层
   查路由表 → 默认网关（如 10.0.0.1）→ ARP 查网关 MAC → 以太网帧发到网关→
   网关路由器查路由表 → 逐跳转发（经过若干 ISP 路由器）→ 到达百度服务器→
   百度回 ICMP Echo Reply → 沿原路或不同路径返回。


## 五、这台服务器的实际部署快照

服务器：腾讯云 CVM，Ubuntu 22.04，IP 101.35.20.131
博客域名：owen571.top

  2 个 systemd 服务：

  owen-blog.service
    ExecStart: node ./dist/server/entry.mjs  (Astro SSR)
    监听: 127.0.0.1:4321

  caddy-owen-blog.service
    ExecStart: caddy run --config .../Caddyfile
    监听: 0.0.0.0:80  → HTTP→HTTPS 重定向
    监听: 0.0.0.0:443 → TLS → 反向代理到 127.0.0.1:4321
    监听: 0.0.0.0:8080 → 额外调试端口（无 TLS）

  Caddy 自动管理 TLS：
    auto_https disable_redirects  # 手动控制重定向逻辑
    Let's Encrypt 自动签发/续期 → 证书位于 /var/lib/caddy/.local/share/caddy/

  请求路径：
    用户浏览器 → (DNS) owen571.top = 101.35.20.131
              → (TCP) 101.35.20.131:443
              → (TLS) Caddy 受理，解密
              → (HTTP) Caddy 转发到 127.0.0.1:4321
              → Astro Node.js 渲染 Markdown → HTML
              → 回程压缩 (zstd) → 加密 (TLS) → 用户浏览器
